import { useState } from 'react'
import {
  Backdrop,
  Box,
  Modal,
  Fade
} from '@mui/material'

const CreateService = ({ open, handleClose, onConfirm }) => {
  return (
    <div>CreateService</div>
  )
}

export default CreateService